# 依赖（本包发布时零运行时依赖，但 checkout 里可能装过东西）
node_modules/

# 隔离测试环境：一个只属于本仓库的 DSH home，由 scripts/dev-home.mjs 创建。
# 它含有从真实 home 复制来的 .credentials.yaml 与运行期会话数据，绝不能提交。
.dsh-test/

# 早期验证用的临时 DSH home
.tmp-dsh-home/

# 如果 checkout 里把插件的 statePath 指向了仓库根
laa/state.json

# 编辑器与系统噪音
.DS_Store
Thumbs.db
*.log
